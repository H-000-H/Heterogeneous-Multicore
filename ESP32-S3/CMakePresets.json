{
  "version": 3,
  "cmakeMinimumRequired": {
    "major": 3,
    "minor": 16,
    "patch": 0
  },
  "configurePresets": [
    {
      "name": "esp-idf",
      "displayName": "ESP-IDF (esp32s3) - 三端通用，依赖已激活的 IDF 环境",
      "description": "需先激活 ESP-IDF 环境：Linux/macOS 运行 export.sh，Windows 运行 export.bat/export.ps1。不硬编码任何平台路径，IDF_PATH/IDF_TOOLS_PATH 等由激活脚本注入 shell 环境。",
      "generator": "Ninja",
      "binaryDir": "${sourceDir}/build/${presetName}",
      "cacheVariables": {
        "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
      },
      "environment": {
        "IDF_TARGET": "esp32s3"
      }
    }
  ],
  "buildPresets": [
    {
      "name": "esp-idf",
      "configurePreset": "esp-idf"
    }
  ]
}
