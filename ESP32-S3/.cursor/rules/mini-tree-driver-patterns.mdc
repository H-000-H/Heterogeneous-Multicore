---
description: mini_tree 驱动层写法 — 对齐 ws2812_drv / w25q64_spi_drv
globs: components/mini_tree/drivers/**/*.{c,h}
alwaysApply: false
---

# mini_tree 驱动层规范

新增或修改 `components/mini_tree/drivers/` 下驱动时，以 `ws2812_drv.c`、`w25q64_spi_drv.c` 为模板。括号风格遵循 `c-brace-style.mdc`。

## 文件骨架

```c
#include "<drv>_drv.h"
#include "device.h"
#include "driver.h"
#include "dev_lifecycle.h"   /* 有 open/close/io 时 */
#include "VFS.h"
#include "dt_config_gen.h"
#include "board_config.h"
#include "compiler_compat.h" /* COMPAT_IGNORE_RESULT */
#include "osal.h"
#include "system_log.h"
/* HAL 头文件按设备选用 */

#include <stddef.h>
#include <stdint.h>
/* <string.h> 仅当需要 strlen/strcmp 等；清零/拷贝用 builtin，见下 */
```

- 公共常量、ioctl、芯片协议宏放在 `*_drv.h`；`.c` 内只保留驱动私有 `#define`。
- `static const char* const kTag = "...";` 用于日志。

## 实例数量与静态池

- 实例数：`#define DRV_COUNT DTC_GEN_COUNT_<COMPAT_MACRO>`（来自 `dt_config_gen.h`，与 DTS `compatible` 节点数一致）。
- 静态池：`s_*_pool[DRV_COUNT]`、`s_*_used[DRV_COUNT]`、`osal_pool_t s_*_pool_ctrl`。
- 大缓冲区、mutex 存储放独立静态数组，按 `pool_idx` 索引，不在 probe 里 malloc。
- 启动初始化：

```c
pre_execution(160)
static void drv_pool_boot_init(void)
{
    osal_pool_init(&s_pool_ctrl, s_used, DRV_COUNT);
}
```

## 内存：GCC builtin，不用 libc memset/memcpy

驱动层清零与块拷贝统一使用 `__builtin_memset` / `__builtin_memcpy`（probe/remove 清实例、SPI 帧拼装等）。

```c
/* ✅ */
__builtin_memset(inst, 0, sizeof(*inst));
__builtin_memcpy(dst, src, len);

/* ❌ */
memset(inst, 0, sizeof(*inst));
memcpy(dst, src, len);
```

仅解析字符串等场景可保留 `strlen`/`strcmp` 并 `#include <string.h>`。

## probe / remove

**probe 顺序（参考 w25q64 / ws2812）：**

1. 读 DTS：`device_get_prop_int` / `device_get_prop_str`；必选属性失败 `goto err_prop` 或 `return VFS_ERR_INVAL`。
2. `pool_idx = osal_pool_claim(...)`；失败 `VFS_ERR_NOMEM`。
3. `__builtin_memset(inst, 0, sizeof(*inst))`；填字段、`pool_idx`。
4. `osal_mutex_create_static` + `device_lc_bind(dev, io_mutex)`。
5. 初始化 HAL / 绑定总线（`bus_client_bind`、`hal_spi_ctx_*` 等）。
6. `device_set_priv`；挂 `dev->ops` 或实例内 `file_operations`。
7. 成功打 `SYS_LOGI` / `DRV_LOGI`；失败路径 `goto err_*`，释放 HAL、destroy mutex、`__builtin_memset`、`osal_pool_release`。

**remove：** `dev_lc_remove_start` → `device_ops_unregister` → `bus_client_unbind` → `dev_lc_remove_drain` → 释放 HAL → `osal_mutex_destroy` → `__builtin_memset` → `osal_pool_release` → `dev_lc_remove_finish`。

注册：

```c
DRIVER_REGISTER(<sym>, "<vendor>,<model>", <probe>, <remove>)
```

薄包装驱动（如 `fft_spi_drv.c`）可只转发 `spi_slave_client_probe/remove`，不重复池逻辑。

## open / close / read / write / ioctl

- 通过 `device_get_priv` / `*_get_drvdata` 取实例；`IS_ERR` / `PTR_ERR` 处理错误指针。
- 并发：`device_lc` + `dev_lc_open_begin/end`、`dev_lc_close_begin/end`、`dev_lc_io_begin/end`；**不要**在 fops 里直接 `osal_mutex_lock`（mutex 由 lifecycle 绑定）。
- `open` 首次打开做硬件 attach / 初始化；`close` 最后一次关闭做 detach。
- 返回 `VFS_OK` / `VFS_ERR_*`；忽略未用参数用 `COMPAT_IGNORE_RESULT` 或 `(void)arg`。

## 设备树分工

| 放 DTS/dtsi | 放驱动代码 |
|-------------|------------|
| `status`、`cs-pin`、`gpio`、`spi-max-frequency` 等板级/实例参数 | SPI opcode、地址字节数、页大小、JEDEC 等芯片协议常量 |
| IP 默认在 `board/dtsi/*.dtsi`；板级在 `board/dts/*.dts` 用 `&label { }` 覆写 | `DTC_GEN_COUNT_*` 由 dtc-lite 按节点数生成 |

可选属性用 `COMPAT_IGNORE_RESULT(device_get_prop_int(...))`，并设合理默认（如 `max-trans-buffer`）。

## 参考文件

- `components/mini_tree/drivers/ws2812/ws2812_drv.c` — 直连 HAL、RMT 参数来自 DTS
- `components/mini_tree/drivers/flash/w25q64_spi_drv.c` — 总线子设备、SPI master、`ioctl` 扩展命令
