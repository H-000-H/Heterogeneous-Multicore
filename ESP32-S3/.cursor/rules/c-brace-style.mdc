---
description: mini_tree C/C++ Allman 括号风格 (函数/结构体/枚举/控制流)
globs: components/mini_tree/**/*.{c,h,hpp,cpp}
alwaysApply: false
---

# Allman 括号风格 (mini_tree)

**凡是有 `{` 的声明与语句，左花括号独占下一行。** 与 `device.h` 中 `struct device`、`spi_client.c` 一致。

## 适用范围

- `struct` / `enum` / `union` 定义
- 函数定义（含 `static`）
- 静态聚合初始化 `= { ... }`
- `if` / `else` / `for` / `while` / `switch` / `do` 复合块
- `switch` 内需要块作用域的 `case`
- `extern "C" {`（C++ 头/源文件中 `{` 仍独占一行）

## 规则

1. **结构体 / 枚举 / 联合体**：名称一行，`{` 下一行，成员缩进，闭合 `};`。
2. **函数**：签名一行，`{` 下一行；简单守卫可单行 `if (!x) return ERR;`。
3. **静态初始化**：`=` 后若开聚合括号，`{` 独占下一行。
4. **控制流**：`if (cond)` / `else` / `else if (cond)` 单独一行，复合体 `{` 下一行；禁止 `if (x) {`。
5. **单语句分支**：仅一条语句时可省略 `{}`，语句另起一行缩进。
6. **`switch`**：`switch (expr)` 后 `{` 下一行；`case` 需局部变量或多语句时，`case LABEL:` 下一行 `{`。

## 示例

```c
struct spi_client
{
    struct file_operations ops;
    int                    pool_idx;
};

enum device_status
{
    DEVICE_STATUS_READY = 0,
    DEVICE_STATUS_PROBED,
};

static const struct file_operations spi_ops =
{
    .open  = spi_open,
    .close = spi_close,
};

static int spi_read(struct device* dev, void* buf, size_t len, uint32_t timeout_ms)
{
    if (!dev)
        return VFS_ERR_INVAL;

    if (ret == VFS_OK)
    {
        ret = bus.read(...);
        ret = spi_xfer_session_end(&ctx, 1, ret);
    }
    return ret;
}

switch (cmd)
{
case SPI_CMD_READ:
{
    ...
    break;
}
default:
    ret = VFS_ERR_INVAL;
    break;
}
```

## 避免

```c
struct foo {          /* ❌ */
    int x;
};

void bar(void) {      /* ❌ */
}

if (ok) {             /* ❌ */
} else {              /* ❌ */
}

static const struct ops ops = {  /* ❌ */
    .open = fn,
};
```
